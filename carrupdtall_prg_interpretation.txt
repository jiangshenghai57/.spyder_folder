declare parameters dealname, and pmtdate

change dir to taxdir

use deallist.dbf
find the deal in deallist
find the first payment of the deal in the deallist

calculate the current period number
get the deal dir from the deallist

run inpcopy.exe in the dealdir + dealname + current period number

"delete the existing cld"? Why?

update the new cld file?

find the mod cld file

